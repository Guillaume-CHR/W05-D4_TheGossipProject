<div class="pricing-header px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center">
  	<h1 class="display-4">The Gossip Project - City n° <%= @city.id =%> </h1>
</div>

<div class="container">
	<div class="card p-3 text-center">
        <div class="card mb-4 shadow-sm">
	        <div class="card-body">
	           	<ul class="list-unstyled mt-3 mb-4">
	              	<li>Name: <%= @city.name %></li>
	              	<li>Zip Code: <%= @city.zip_code %></li>
	            </ul>
          	</div>
        </div>
	</div>

	<%= render :partial => 'gossips/gossips', :locals => {:tbl_gossip => Gossip.where(user_id: User.where(city_id: @city.id).ids), i: 0} %>

</div>